import { SearchBar } from 'components/search';

export default {
  title: 'Components/SearchBar',
  component: SearchBar,
};

export const Default = {
  args: {
    dispatch: () => undefined,
  },
};
